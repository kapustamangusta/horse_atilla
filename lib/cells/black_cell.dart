import 'package:flutter/material.dart';

class BlackCell extends StatelessWidget {
  const BlackCell({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 50,
      width: 50,
      color: Colors.black,
    );
  }
}
